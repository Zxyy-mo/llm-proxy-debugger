# 开发与提交约定

以下约定适用于本项目后续新增或修改的代码与 Git 提交。

## Git 提交使用中文

- 提交标题和说明正文使用中文，说明实际的行为变化、修改原因和验证结果。
- 可以保留 Conventional Commits 的类型和作用域，以及必要的代码标识、协议名和产品名。例如：`feat(provider): 增加 OpenAI 兼容上游接入预设`。
- 验证说明区分静态检查、受控 mock 测试和真实接口验收，按实际执行结果填写。

## 重要函数和关键步骤使用中文注释

- 重要函数需要中文注释，说明职责以及不直观的输入、输出或使用约束。Go 文档注释以对应标识符开头，后接中文说明。
- 关联推断、协议转换、路由与故障切换、并发和状态迁移、持久化与恢复、隐私处理、重放等关键步骤，需要说明处理原因、边界条件、不变量或失败行为。
- 修改关键逻辑时同步更新相关注释，保证注释与实现一致。协议字段、API、函数名和第三方产品名保留原文。
- 注释着重解释原因和约束，避免逐行复述简单代码。已有代码在触及相关逻辑时逐步补齐。

## 开发前确认范围

- [实施路线](IMPLEMENTATION_ROADMAP.md) 记录已交付能力和已确认的下一阶段方向。
- [契约索引](docs/README.md) 记录当前实现的接口、状态和边界；规划中的能力应在实现与验收后再纳入已交付清单。
- 具体开发入口及检查命令见 [交接记录](HANDOFF.md)。
