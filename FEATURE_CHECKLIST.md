# 功能与交付清单

更新日期：2026-09-11。当前开发分支：`feat/agent-debugger-workflow`。这里记录可使用的功能；Provider 本身未提供的能力和完整 Agent 执行器不计为已实现。

## 实际排障流程

- [x] 当前会话请求关键词/状态筛选、耗时排序，手机和短横屏可用；筛选保留选择和完整因果图。
- [x] 请求行聚焦可读节点；手动镜头和显示全图不被指标更新打断。
- [x] 直接进入上下文、编辑重放和来源/结果；同一调查保留草稿、请求头和临时凭证。
- [x] 草稿与执行状态独立；重置、切换或刷新后仍能找到并取消进行中的重放。
- [x] 创建响应丢失及隐式重试失败时保留同一提交和幂等键；恢复不重复执行。
- [x] 来源/结果状态、错误、可读输出和可信指标并列评估；运行中的比较随结束更新。
- [x] 返回历史保留查询和页码；首次加载失败、无数据、无匹配和缺失来源有明确处理。
- [x] 大整数、小数和指数的修改差异保真；深层/超大 JSON 不会因格式化或比较冻结界面。

验收见 [排障流程验证](output/playwright/agent-debug-loop/verification.md)；操作步骤见 [使用指南](docs/USAGE.md)。

## 原有基础与 M1

- [x] HTTP/SSE 转发，Chat Completions、Responses、Anthropic Messages 的 JSON/SSE 观测。
- [x] 显式会话与父调用标识、唯一历史前缀匹配、分支、缺失/迟到父调用、歧义与防循环。
- [x] 可交互调用图、实时同步、刷新/重连恢复选择、窄屏与短屏布局。
- [x] HTTP 请求断点、倒计时、正文/允许头编辑、校验、保存、放行、取消和版本冲突控制。
- [x] 完整原始/出站快照、安全 cURL、精确正文文件导出。
- [x] 单次模型请求重放、幂等键、临时凭证、超时/取消及重放来源。

## M2：完整响应与可信指标

- [x] JSON/SSE 完整文件捕获、分段浏览/下载、直达末段、缺失文件与捕获失败提示。
- [x] gzip 解压标记、非文本 Base64 预览、UTF-8 安全截断和大整数保真。
- [x] 输入/输出/思考 Token 的 `usage / estimated / unknown` 标记；显式零值具有权威性。
- [x] 出站首响应头与首有效内容时间，排除断点等待；失败/取消/无法观测时显示未知。
- [x] 重放响应与来源响应对比。
- [x] 超大 SSE 事件的观测受限提示；原生响应转发和文件保真不受解析上限影响。

## M3：上下文与规则

- [x] 基于真实父调用或用户选择的上下文比较，默认使用实际出站快照。
- [x] 消息序列对齐、新增/删除/保留、系统指令和工具定义变化。
- [x] 远端历史缺口、过大对齐矩阵及输入限制提示；不会凭变化断定“模型压缩了历史”。
- [x] Anthropic `system`、Chat `system/developer` 消息、Responses `instructions` 分别注入。
- [x] 规则数值优先级、同优先级稳定顺序、编辑/启停/删除。

## M4：记录与出站脱敏

- [x] 独立的记录/出站开关，默认邮箱和手机号模式，自定义正则校验。
- [x] 会话及上游/客户端鉴权范围内稳定占位符，覆盖 JSON 字符串、历史、当前消息和工具结果；清理保留仍被引用的还原映射。
- [x] 明确的原文保留与受控还原策略；配置冻结到请求进入时。
- [x] 记录脱敏先于预览截断；流式输出采用结束后的脱敏投影，未完整工具参数不会作为安全完整数据展示。
- [x] 凭证值不进入快照、导出、持久配置或浏览器存储。

## M5：本地持久化与历史

- [x] SQLite 元数据、独立正文文件、规则/路由/脱敏策略和关联索引恢复。
- [x] 重放登记和幂等键同步落盘后才执行；保存失败返回 503 且不发送；重启后同一已记录动作不再执行。
- [x] 原等待/运行请求标记中断，Token 与时序未知，无自动补发。
- [x] 关键词、Trace/Response ID、模型、状态、会话筛选及有界分页。
- [x] 删除和按会话/时间清理；活动请求保护、关联引用保留、正文文件删除。

## M6：工具与外部 tracing

- [x] 文本/函数/API 内置工具的可见调用记录，参数、后续结果和状态。
- [x] 独立工具图节点与详情，结果所属 Trace 可定位。
- [x] `POST /api/tool-spans` 接收 `mcp / agent / tool` 执行记录；界面提供接入示例。
- [x] 不同 Span ID 不相互覆盖；真实开始/结束时间计算耗时，无上报时保持未知。
- [x] 工具结果与 Span 持久化，恢复后图谱与详情一致。

## M7：Provider、路由与转换

- [x] Provider CRUD 表单/API、环境变量凭证、能力开关和配置持久化。
- [x] 模型精确/尾部 `*` 匹配、优先级、别名、默认上游。
- [x] 每条路由最多 3 个显式备用 Provider，仅连接错误或 502/503/504 且未向客户端输出时尝试。
- [x] 原生透传以及独立 Anthropic/Responses 文本、函数工具到 Chat Completions 的 JSON/SSE 转换。
- [x] 不支持的转换语义明确拒绝；转换前/后响应分开捕获并标记。
- [x] 出站重放固定原 Provider/地址，不再路由、注入或转换；配置目的地改变时拒绝。
- [x] `/v1` Base URL、挂载前缀与转义路径保真；上游 TLS 默认校验。

## M8：指定 WebSocket 与历史接口

- [x] Responses `response.create` 调用记录、`stream_id` 分组、ID 关联、工具和指标。
- [x] 首个请求按模型选 Provider；同一连接保持目的地，后续不兼容路由明确拒绝。
- [x] 完成、取消、失败和断开；JSONL 保存完整原始文本消息内容。
- [x] 按 Provider 与已知 Response ID 查询已保存响应；不新增虚假的模型执行。
- [x] 历史查询使用配置鉴权或本次临时密钥，并遵循记录脱敏。
- [x] UI 和契约清楚区分 mock 已验证能力与真实 Provider 尚未验证能力。

## 验收证据

- [x] Go race 全量测试与 vet、Vue 类型检查及生产构建。
- [x] 浏览器 25 项功能断言及 Provider/历史查询/工具图手动验证。
- [x] 320×568、390×844、844×390、768×1024、1440×900 五种目标视口；无已测控件不可达或水平溢出。
- [x] 实际进程中断/重启后的 13 项检查；12 份客户端/上游响应文件哈希一致。
- [x] 合成历史删除、父引用处理、6 个相关正文文件清理。
- [x] 用户指定 `glm-5.3-flash` 的 JSON/SSE 真实中转，200、下载字节一致、凭证隔离。

详见 [验证报告](output/playwright/foundation/verification.md) 和 [results.json](output/playwright/foundation/results.json)。

## 当前边界

协议转换的完整多模态/服务端状态、WebSocket 帧编辑/重放、账号历史枚举、自动 SDK 插桩、完整 Agent/MCP 工作流执行、负载均衡与高规模数据库查询均未实现。当前实现提供上述明确子集。

SQLite 使用合并元数据快照和内存筛选；JSON/累积文本处理仍消耗内存；异常中止存在尚未落盘元数据窗口。详见 [存储与能力契约](docs/README.md)。模型没有公开的内部推理不属于可捕获数据。
