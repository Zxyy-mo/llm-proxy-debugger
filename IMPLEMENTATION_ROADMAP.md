# LLM Debug Gateway 后续实施路线

更新日期：2026-09-10。依据：功能清单、当前工作区代码、已完成任务及验证记录。M1 已于 2026-09-10 完成；以下未完成能力仍保持待办状态。

## 当前判断

已具备流量捕获、三种协议解析、会话关联、调用图、请求断点与编辑放行，以及安全 cURL 导出与单次模型请求重放（M1，见 [验证记录](output/playwright/replay/verification.md)）。下一步是 **M2：完整响应与可信指标**，让重放和断点观察到的响应可以完整查看、下载，并区分 Token 的准确/估算/未知与首内容时间。

M2 可以复用重放记录、请求快照与 SSE 原始落盘；实现时先定义完整响应的保存与查询接口，再补齐指标来源标记。

## 核对发现

| 检查项 | 当前代码事实 | 对下一步的影响 |
| --- | --- | --- |
| 完整请求 | `internal/store/requests.go` 保存原始/出站正文，支持 Base64 | 可以作为重放源；必须使用原始字节，不能从格式化后的界面文本反向生成 |
| 请求头与鉴权 | M1 已保存可转发请求头、凭证名称与方案，凭证值仅用于本次重放 | M2 完整响应接口复用已有捕获与重放关系，不把凭证引入新增元数据 |
| 请求地址 | 原始 URL 通常为相对路径，出站 URL 已含上游前缀 | cURL 必须区分 Gateway 与上游；不能使用前端 Vite 端口，也不能二次拼接上游前缀 |
| 动态规则 | 每次进入 `handleHTTP` 都执行 `prepareRequest` | 重放已经修改过的出站快照时，应默认保持快照内容，避免再次注入或再次进入断点 |
| 来源关系 | 日志/图谱已有 `replay.of`，重放记录已有 `replay_of` | M2 可直接据此比较来源与重放响应；继续把重放来源与会话父子关系分开 |
| 响应数据 | JSON 响应只限量缓存；SSE 原始流落盘，但没有完整响应查询入口 | 完整响应对比、下载与持久化仍有缺口 |
| Token/耗时 | 有累计值与等待/上游耗时，没有用量来源和首内容时间 | 需要区分准确、估算、未知；TTFT 计时应从实际出站开始，排除人工等待 |
| Diff | 当前是同一次请求原始/修改后的 JSON 字段对比 | 相邻调用的消息对齐、增删、system/tools 变化仍需单独实现 |
| 规则协议适配 | 注入固定写顶层 `system` | Chat Completions/Responses 的注入位置仍需按协议适配，不能把三种响应解析支持等同于三种请求修改支持 |
| 存储与工具 | 会话/索引/规则在内存；工具主要是计数、报文和指纹 | 后续需要持久化、结构化工具记录以及真实 tracing 数据源 |

## 按依赖安排的交付顺序

| 顺序 | 范围 | 完成时用户可以做到 | 关键依赖/验收 |
| --- | --- | --- | --- |
| M1 · P1（已完成） | 安全 cURL、单次模型请求重放 | 从已完成请求导出命令，选择原始/出站内容，按需编辑后重新发送并查看新调用 | 已交付：本次凭证、原文字节保真（含真实 curl 回放）、一次动作一个 Trace、无重复注入、重放来源标注、取消与超时 |
| M2 · P1 | 完整响应与可信指标 | 查看/下载完整 JSON/SSE 响应，知道 Token 是否准确，以及等待、首内容和上游耗时 | 大正文不受展示上限影响；准确/估算/未知不混用；错误或无输出时 TTFT 为未知 |
| M3 · P1 | 历史上下文 Diff、规则协议适配与优先级 | 对比真正相邻的调用，查看消息增删与 system/tools 变化；对三种协议正确注入并排序规则 | 按父引用/用户选择比较；数组插入不导致整段误报；缺失远端上下文明确显示缺口 |
| M4 · P1 | 记录脱敏与出站脱敏 | 分别选择展示脱敏和发送脱敏；跨历史、当前消息和工具结果复用稳定占位符 | 原文与展示投影分离；映射有会话范围；还原与原文保留策略明确 |
| M5 · P1 | 持久化与历史查询 | 重启后找回会话、规则、报文与关联图；按条件查找和清理历史 | 建议 SQLite；基于 M2/M4 的数据与保存策略建模；重建索引；中断请求标记中断，不自动补发 |
| M6 · P2 | 工具节点、MCP/Agent tracing | 看到独立工具调用、输入输出、状态与实际耗时，并定位执行失败 | 先结构化 API 可见数据，再接入真实工具执行 Span；未观测到的耗时显示未知 |
| M7 · P2 | 多 Provider 与协议转换 | 按模型/规则路由，管理上游与凭证，按明确策略处理失败 | 支持能力矩阵；字段/usage/tool 语义转换；不能在已输出部分响应后无声重试 |
| M8 · P2 | LLM WebSocket 帧与 Provider 历史适配 | 把选定协议的 WebSocket 请求纳入调用图；查询所支持 Provider 暴露的历史 | 按具体协议/官方 API 验收；不承诺所有 Provider 都能提供账号历史 |

M4 的原文保存策略应在 M5 建库前确定，避免后续重写存储。M6 的真实工具执行数据是完整 Agent 流程重放的前提；届时需分别定义“复用已记录工具结果”和“重新执行工具”的模式。M1 只发送单次模型 API 请求，不调度本地 Agent/MCP；请求中的 Provider 内置工具仍可能由 Provider 执行。

M2 可以采集本地 DNS/连接/TLS、首响应字节和首有效内容时间；Provider 排队与纯模型计算耗时只有在 Provider 提供对应数据时才能单独展示。M3 对“压缩/摘要”的判断也必须展示依据，推断要明确标注。

工程任务随各阶段推进：维护 `docs/contracts/` 中的协议/API 契约及本地 Trellis 规范，保留回归测试和浏览器证据。2026-09-10 已获授权提交并推送现有成果；M2 本轮范围为规划。

## M1 的交付结果

任务文档：[Safe request cURL export and single-request replay](docs/contracts/request-replay.md)。状态：已实现并验证，契约见 [request-replay.md](docs/contracts/request-replay.md)。以下为交付时确认的范围：

- 从“原始 / 出站”入口选择来源；默认使用实际出站快照。被取消、从未出站的请求不显示虚构的出站内容。
- 补齐生成命令和执行重放所需的来源地址、可转发请求头、鉴权类型与缺失字段信息。旧快照缺的信息必须提示，不能猜测。
- cURL 使用凭证占位符，不复制遮盖后的字符串充当真实 Key。命令、URL、正文和文件名正确转义；大正文和二进制支持精确保真的文件导出路径。
- 页面可把快照复制为新草稿，按需编辑、校验、补充本次鉴权后主动发起重放。历史记录保持不变。
- 当前没有 Provider 凭证配置，默认采用仅供本次重放使用的鉴权输入；不把凭证存进日志、历史快照或浏览器持久存储。
- 一次重放动作生成一个新 Trace，记录 `replay_of`；网络重试或重复提交同一动作不能产生重复执行。再次主动重放可以创建另一个 Trace。
- 沿用请求记录、协议解析、实时事件和结果详情；出站快照重放不二次应用规则或上游路径前缀。
- 执行具有明确的超时和取消机制。服务重启、页面刷新和历史加载不会自动发送模型请求。

验收已在本地 mock upstream 完成：逐字节核对请求、特殊字符与大整数、头和鉴权保留、URL 前缀、JSON/SSE、重复提交、取消、错误记录和图谱来源，以及 7 种视口可达性。签名类鉴权、完整 Agent 流程重放和重放记录持久化不在 M1 范围内。

## 下一项 M2 的明确范围

任务文档：[Complete responses and trustworthy metrics](docs/plans/m2-response-metrics/prd.md)（状态：待开始，已交接）。接手说明见 [HANDOFF.md](HANDOFF.md)。

- 完整响应查看与下载：JSON 响应不受 `-maxbody` 限制地保存或可从落盘文件读取；SSE 已有原始落盘，需要查询与下载入口，并与请求快照、重放记录关联。
- Token 来源标记：区分 Provider usage（准确）、字符估算（估算）和无法确定（未知），在日志、节点和详情中一致展示，不混用。
- 时序：从实际出站开始计 TTFT（首响应字节与首有效内容），排除人工/规则等待；错误或无输出时显示未知；Provider 排队与模型计算耗时只有在 Provider 返回对应数据时才单独展示。
- 验收：大响应逐字节一致、SSE 与 JSON 都有下载入口、重放结果可与来源请求的响应对比、指标标记在三种协议下正确，并保留小屏检查。

## 整体功能完成的判定

- 每个里程碑同时交付后端、可用界面、接口/规范及验证记录；只有接口或开关不计完成。
- 第一阶段完成：完整观察数据、可信指标和可恢复历史可用。
- 第二阶段完成：断点编辑、单次重放、上下文对比、规则与脱敏策略可用。
- 第三阶段完成：约定范围内的工具执行、Provider 和传输协议有真实数据与适配验证。
- Provider 账号历史按能力矩阵验收；未公开的模型内部推理属于产品边界，不作为待实现功能。
- 工具实际耗时或因果关系没有证据时显示未知，不通过时间间隔或相邻位置冒充真实 tracing。
