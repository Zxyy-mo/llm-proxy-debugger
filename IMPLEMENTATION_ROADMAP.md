# 实施路线与当前交付

更新日期：2026-09-11。**`feat/agent-debugger-workflow` 是当前持续开发分支。** 在 M2–M8 的约定实现之上，已完成会话脱敏/清理、重放登记可靠性、完整响应浏览，以及真实排障流程的完善。

本轮继续交付四层调用模型及 CPA、New API、Sub2API、vLLM 的兼容接入配置、能力声明和模型发现。真实平台实例尚未联调，受控验证与实际能力分开记录。

## 最近完成的完善

先通过 [基础修复验收](output/playwright/foundation-refinement/verification.md)，再打通“筛选异常调用 → 检查父上下文 → 修改并重放 → 评估结果 → 返回调查”。草稿、执行状态和查看对象各有明确归属；丢响应不会静默变成新的一次调用。来源缺失、正在执行、估算与未知指标均保持可见。

该流程在 70 次合成 Agent 调用、七种视口、真实浏览器网络故障和取消操作下验证。完整结果见 [排障流程验收](output/playwright/agent-debug-loop/verification.md)，行为约定见 [操作契约](docs/contracts/operator-workflow.md)。

## 交付状态

| 阶段 | 当前可用行为 | 对应契约 |
| --- | --- | --- |
| 基础与 M1 | 三协议观测、会话/父子关联、图谱、HTTP 断点、安全 cURL、单次重放 | [关联](docs/contracts/conversation-graph.md)、[断点](docs/contracts/request-interception.md)、[重放](docs/contracts/request-replay.md) |
| M2 | 完整 JSON/SSE 文件、下载、Token 来源、排除等待的首响应时序、来源响应对比 | [响应与指标](docs/contracts/response-metrics.md) |
| M3 | 相邻上下文对齐、system/tools 变化、远端缺口、协议注入与规则优先级 | [上下文与规则](docs/contracts/context-rules.md) |
| M4 | 记录/出站脱敏、稳定替换、原文保留及受控还原 | [脱敏、历史与工具](docs/contracts/privacy-history-tools.md) |
| M5 | SQLite 元数据、报文文件恢复、搜索/清理、中断状态与重放幂等恢复 | 同上 |
| M6 | 独立工具节点、参数与结果、MCP/Agent/tool Span 接收和真实耗时 | 同上 |
| M7 | Provider 管理、模型路由/别名、显式备用上游、文本/函数协议转换 | [Provider 与传输](docs/contracts/providers-transports.md) |
| M8 | Responses WebSocket 调用观测，已保存 Response 按 ID 查询 | 同上 |
| 调用分层 | 明确 Run 分组、未关联请求、独立 Attempt/出站内容/计时、重放新任务、历史兼容 | [四层调用](docs/contracts/call-layers.md) |
| 兼容接入 | 常用预设、三态能力、模型发现、列表应用路由及配置失效控制 | [兼容上游](docs/contracts/compatible-providers.md) |

每一项都有后端、操作界面、契约和验证。详细勾选项见 [FEATURE_CHECKLIST.md](FEATURE_CHECKLIST.md)；实测证据见 [foundation/verification.md](output/playwright/foundation/verification.md)。

## 已确定的实现选择

1. **保持真实报文与展示投影的区分。** 完整请求/响应按文件保存；列表和实时事件只传有界内容。gzip、脱敏、转换前后及 WebSocket JSONL 都有明确标记。
2. **指标带来源。** Token 分 usage、字符估算、未知；计时从上游开始，不推算 Provider 内部排队或纯模型时间。超大 SSE 解析受限时不保留“完整准确观测”的假象。
3. **关系需要证据。** 会话归属、真实父子、重放来源、工具执行分别记录；远端历史、歧义和缺失节点不靠时间补猜。
4. **策略在请求进入时确定。** 规则、脱敏与所选目的地的变更影响后续请求；等待中的请求不因管理面板改动改投其他上游。
5. **支持子集显式化。** 原生透传承载 Provider 自身协议；转换只覆盖明确可映射的文本/函数语义，遇到不支持字段或输出明确失败。
6. **恢复状态，不恢复执行。** 已落盘记录、索引、配置、工具和幂等键恢复；之前活动的模型请求/重放标记中断，不自动执行。
7. **先提供可接入的 tracing 契约。** 外部应用主动上报工具执行 Span，网关不自行调度工具，也不把 API 间隔当工具耗时。

这些选择及已知容量边界也记录在 [foundation 设计记录](docs/plans/foundation.md)。

## 当前版本的能力边界

- M6 提供结构化工具记录和 Span 接口，尚无各家 SDK 的自动插桩包或完整 Agent 流程重放执行器。
- M7 提供模型匹配、优先级、别名及有限故障切换；尚无负载均衡调度，协议转换也不宣称覆盖多模态、内置工具或远端状态。
- M8 的历史为按已知 ID 读取保存的 Response，非账号枚举；WebSocket 为 Responses 调用观测，帧编辑/帧重放未实现。
- 元数据目前是 SQLite 单行 JSON 合并快照，查询在内存筛选，正文另存文件；不是海量记录的 SQL 查询架构。
- HTTP JSON 与流式文本累积仍使用内存。普通元数据默认约 200 ms 合并写，突然中止可能丢失最近状态；新重放的登记和幂等键同步落盘后才允许执行。
- 模型未公开的内部推理、未经过网关也未上报的工具执行，不属于可推断的已知数据。

下面已确认的调用分层和通用兼容接入已完成本轮实现。下一步可基于实际 CPA/New API/Sub2API/vLLM 地址开展真实联调；增量 SQL 存储和完整 Agent 执行器仍按独立需求规划。

## 四层模型与兼容接入的交付范围

2026-09-11 用户确认并授权实施以下方向。当前版本按下列边界交付，精确字段和行为以对应契约为准。

### 四层调用模型

采用 **会话 → 一轮任务 → 模型请求 → 上游执行尝试** 的分层：

| 层级 | 含义 | 记录重点 |
| --- | --- | --- |
| 会话（Session / Thread） | 一段持续的用户对话 | 会话标识、来源及其包含的任务 |
| 一轮任务（Run） | 一次用户输入或明确触发的任务，可产生多次模型调用 | 任务标识、所属会话、任务内调用的关联证据 |
| 模型请求（Request） | 客户端发起的一次逻辑模型调用 | 输入/输出、上下文、工具调用与来源关系 |
| 上游执行尝试（Attempt） | 该请求对具体上游的一次实际发送，包括故障切换或重试 | 目标上游、出站内容、状态、错误与实际观测的耗时 |

- `trace_id` 保留 Request 语义，已引入独立 `run_id`、归属证据和 `route.attempts[].id`；原有 API 和图谱结构保持兼容，元数据写 v3 并读取 v1/v2/v3。
- 会话归属、一轮任务归属、请求因果、上游尝试和工具执行分别记录。共享会话或时间接近不能作为任务边界或父子关系的充分证据；无法识别时明确标注未知或未关联。
- Run 可以包含并发或分支请求。工具执行 Span 与上游 Attempt 保持独立；单次模型重放创建新 Run/请求并保留来源关系，原始报文字节不为分组而改写。
- 网关只能直接证明它向所配置上游发出的尝试。CPA、New API、Sub2API 等中转内部的供应商切换或重试，需要对方提供证据才能进一步呈现。
- 四层模型是观察与调试的数据组织方式，完整 Agent/MCP 工作流重放仍需要独立的执行机制。

### 优先接入的兼容服务

优先覆盖 **CPA、New API、Sub2API，以及 vLLM 的 OpenAI 兼容接口**。围绕这些服务暴露的接口建立通用接入能力，利用其中转或推理服务覆盖的模型范围。

- 复用现有自定义 `base_url`、鉴权和模型路由能力，以通用协议处理为基础，再补齐各平台必要的配置与差异适配。
- 首批验收重点为 OpenAI 兼容 Chat Completions 的 JSON/SSE、函数工具调用与结果、用量和错误；模型列表、路径拼接、模型别名与鉴权也纳入接入检查。
- Responses、Anthropic Messages、历史查询、WebSocket，以及其他接口按具体实例、版本和实际开放能力建立支持矩阵。分别标注原生透传、结构化观测、协议转换和调试操作的支持情况。
- 保留明确的能力开关和不支持原因。平台名称或“OpenAI 兼容”描述不自动等于所有接口、字段和语义都兼容。
- 四类服务已有可复现配置示例和通用协议受控测试；目前没有这些实例的真实地址/凭证，真实接入验收保持待验证。模型列表查询成功不自动升级其他能力。

后续提交与代码注释遵循 [开发与提交约定](CONTRIBUTING.md)：提交标题/正文、重要函数和关键步骤的注释使用中文。

## 验证范围

Go race/vet 和 Vue 构建通过；浏览器覆盖功能、桌面/窄屏/短屏布局；实际进程重启验证报文哈希、配置、图谱/Span 和幂等状态；真实 `glm-5.3-flash` 测试验证 Chat Completions 的 JSON/SSE 中转与完整下载。

Responses WebSocket、Provider 历史、转换与故障切换有受控 mock/协议测试。真实中转是否提供这些能力仍取决于其接口，当前报告不将 mock 通过等同于该 Provider 已通过。
